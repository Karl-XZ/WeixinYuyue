# 邮件错误代码对照表

## 常见错误及解决方案

### 1. EAUTH - 认证失败

**错误信息：**
```
邮件发送失败：Invalid login: 535 Error: authentication failed
```

**原因：**
- 邮箱账号或授权码错误
- 授权码已过期或被重置
- 163邮箱SMTP服务未开启

**解决方案：**
1. 检查 `mail_lib.js` 中的配置：
   - 邮箱：your_email@163.com
   - 授权码：your_smtp_auth_code_here
2. 登录163邮箱，重新获取授权码：
   - 设置 -> POP3/SMTP/IMAP
   - 开启SMTP服务
   - 获取新的授权码
3. 更新 `mail_lib.js` 中的授权码

---

### 2. ETIMEDOUT - 连接超时

**错误信息：**
```
邮件发送失败：Connection timeout
```

**原因：**
- 网络连接问题
- 防火墙拦截
- SMTP服务器地址错误

**解决方案：**
1. 检查云函数的网络配置
2. 确认SMTP服务器地址：smtp.163.com
3. 确认端口：465
4. 检查云函数是否有外网访问权限

---

### 3. ECONNREFUSED - 连接被拒绝

**错误信息：**
```
邮件发送失败：Connection refused
```

**原因：**
- SMTP端口错误
- 服务器不可达

**解决方案：**
1. 确认端口配置：465（SSL）或 25（非SSL）
2. 确认 secure 配置：true（使用SSL）
3. 尝试更换端口

---

### 4. ENOTFOUND - 找不到服务器

**错误信息：**
```
邮件发送失败：getaddrinfo ENOTFOUND smtp.163.com
```

**原因：**
- DNS解析失败
- SMTP服务器地址错误

**解决方案：**
1. 检查SMTP服务器地址拼写
2. 确认云函数有DNS解析能力
3. 尝试使用IP地址代替域名

---

### 5. EMESSAGE - 邮件内容问题

**错误信息：**
```
邮件发送失败：Message failed
```

**原因：**
- 邮件内容格式错误
- 收件人地址无效

**解决方案：**
1. 检查收件人邮箱格式
2. 检查邮件主题和内容
3. 确认邮件大小未超限

---

### 6. EENVELOPE - 信封错误

**错误信息：**
```
邮件发送失败：Bad sender address
```

**原因：**
- 发件人地址格式错误
- 发件人地址与认证账号不匹配

**解决方案：**
1. 确认发件人地址：your_email@163.com
2. 确认发件人地址与SMTP认证账号一致

---

### 7. ESMTP - SMTP协议错误

**错误信息：**
```
邮件发送失败：SMTP error
```

**原因：**
- SMTP协议配置错误
- 服务器不支持当前配置

**解决方案：**
1. 检查secure配置（SSL/TLS）
2. 尝试不同的端口和加密方式
3. 查看SMTP服务器文档

---

## 调试步骤

### 1. 查看完整错误信息

在云函数日志中查看：
```
[mail_lib] 邮件发送失败: Error: ...
[mail_lib] 错误代码: EAUTH
[mail_lib] 错误信息: Invalid login
```

### 2. 本地测试

运行测试脚本：
```bash
cd cloudfunctions/mcloud
node test_mail.js
```

如果本地测试成功，说明配置正确，问题可能在云端环境。

### 3. 检查配置

确认 `mail_lib.js` 中的配置：
```javascript
const host = 'smtp.163.com';
const port = 465;
const secure = true;
const user = 'your_email@163.com';
const pass = 'your_smtp_auth_code_here';
```

### 4. 测试不同配置

如果当前配置不工作，尝试：

**配置1：SSL端口465**
```javascript
host: 'smtp.163.com',
port: 465,
secure: true
```

**配置2：TLS端口587**
```javascript
host: 'smtp.163.com',
port: 587,
secure: false,
tls: {
  rejectUnauthorized: false
}
```

**配置3：非加密端口25**
```javascript
host: 'smtp.163.com',
port: 25,
secure: false
```

---

## 163邮箱特殊说明

### 授权码获取步骤

1. 登录163邮箱网页版
2. 点击"设置" -> "POP3/SMTP/IMAP"
3. 开启"POP3/SMTP服务"或"IMAP/SMTP服务"
4. 点击"客户端授权密码"
5. 通过手机验证后获取授权码
6. **注意：授权码不是邮箱登录密码！**

### 发送限制

163邮箱有以下限制：
- 每天发送数量限制（免费版约200封）
- 每小时发送数量限制
- 单封邮件大小限制（约15MB）
- 收件人数量限制（约100个）

如果超过限制，会收到错误：
```
邮件发送失败：Quota exceeded
```

**解决方案：**
- 升级到VIP邮箱
- 使用企业邮箱
- 更换邮件服务商

---

## 其他邮件服务商配置

### QQ邮箱

```javascript
host: 'smtp.qq.com',
port: 465,
secure: true,
auth: {
  user: 'your-qq@qq.com',
  pass: 'your-authorization-code'
}
```

### Gmail

```javascript
host: 'smtp.gmail.com',
port: 465,
secure: true,
auth: {
  user: 'your-email@gmail.com',
  pass: 'your-app-password'
}
```

### 阿里云邮箱

```javascript
host: 'smtp.aliyun.com',
port: 465,
secure: true,
auth: {
  user: 'your-email@aliyun.com',
  pass: 'your-password'
}
```

---

## 快速诊断命令

### 测试SMTP连接

```bash
# 使用telnet测试（Windows）
telnet smtp.163.com 465

# 使用PowerShell测试
Test-NetConnection -ComputerName smtp.163.com -Port 465
```

### 查看nodemailer版本

```bash
cd cloudfunctions/mcloud
npm list nodemailer
```

### 更新nodemailer

```bash
npm update nodemailer
```

---

## 现在的改进

✅ **已添加详细错误日志**

现在当邮件发送失败时，你会在前端看到具体的错误信息，例如：
- "邮件发送失败：Invalid login: 535 Error: authentication failed"
- "邮件发送失败：Connection timeout"
- "邮件发送失败：getaddrinfo ENOTFOUND smtp.163.com"

这样可以快速定位问题！

---

## 下一步

1. **重新上传云函数**
   - 右键 `cloudfunctions/mcloud`
   - 选择"上传并部署：云端安装依赖"

2. **测试发送验证码**
   - 在小程序中填写邮箱
   - 点击"发送验证码"
   - 查看错误提示

3. **根据错误信息解决**
   - 参考本文档的错误代码对照表
   - 按照解决方案操作

4. **把错误信息发给我**
   - 如果还是无法解决
   - 把完整的错误信息发给我
   - 我会帮你分析
