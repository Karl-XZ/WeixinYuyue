# 快速部署指南

## 一、前置准备

### 1. 确认环境
- 已安装微信开发者工具
- 已配置云开发环境
- 已有管理员权限

### 2. 备份数据（重要！）
在进行任何操作前，请先备份：
- 云数据库（特别是 meet 表）
- 云函数代码

## 二、部署步骤

### 步骤1：安装依赖

打开命令行工具，进入云函数目录：

```bash
cd cloudfunctions/mcloud
npm install
```

等待安装完成，确保 nodemailer 已成功安装。

### 步骤2：测试邮件发送（可选但推荐）

在云函数目录执行测试脚本：

```bash
node test_mail.js
```

如果看到 "✅ 邮件发送成功！"，说明邮件配置正确。
然后检查邮箱 your_email@163.com 是否收到测试邮件。

### 步骤3：上传云函数

在微信开发者工具中：

1. 找到左侧文件树中的 `cloudfunctions/mcloud` 文件夹
2. 右键点击该文件夹
3. 选择 "上传并部署：云端安装依赖"
4. 等待上传完成（可能需要几分钟）

### 步骤4：验证部署

#### 4.1 检查云函数
在微信开发者工具的"云开发"控制台：
1. 进入"云函数"页面
2. 找到 mcloud 函数
3. 查看"版本管理"，确认最新版本已部署

#### 4.2 检查数据库
在"云开发"控制台的"数据库"页面：
1. 会自动创建 `bx_mail_verify` 集合（邮箱验证码）
2. `bx_meet` 集合会自动添加新字段（MEET_EMAIL等）

### 步骤5：测试功能

#### 测试教师注册
1. 打开小程序
2. 进入教师端
3. 点击"注册"
4. 填写信息：
   - 姓名：测试教师
   - 手机：13800138000
   - 密码：123456
   - 教师密码：123456（默认值，可在后台修改）
   - 邮箱：你的测试邮箱
5. 点击"发送验证码"
6. 检查邮箱，输入收到的6位验证码
7. 提交注册

#### 测试预约提醒
1. 用学生账号登录
2. 选择刚注册的教师的课程
3. 完成预约
4. 检查教师邮箱，应该收到预约提醒邮件

## 三、常见问题

### Q1: npm install 失败
**解决方案：**
```bash
# 清理缓存
npm cache clean --force

# 使用淘宝镜像
npm install --registry=https://registry.npmmirror.com

# 或者使用 cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com
cnpm install
```

### Q2: 邮件发送失败
**可能原因：**
1. 授权码错误
2. SMTP服务器被防火墙拦截
3. 邮箱未开启SMTP服务

**解决方案：**
1. 检查 `mail_lib.js` 中的配置
2. 确认163邮箱已开启SMTP服务
3. 重新获取授权码
4. 尝试使用其他邮箱服务商

### Q3: 验证码收不到
**检查项：**
1. 邮箱地址是否正确
2. 检查垃圾邮件文件夹
3. 查看云函数日志，确认是否发送成功
4. 检查邮箱是否有接收限制

### Q4: 老教师数据没有邮箱
**解决方案：**
有两种方式：
1. 让老教师重新注册（推荐）
2. 在数据库中手动添加邮箱字段：
   ```javascript
   // 在云开发控制台的数据库中，找到 bx_meet 集合
   // 为每个教师记录添加：
   {
     "MEET_EMAIL": "teacher@example.com",
     "MEET_EMAIL_VERIFIED": 1,
     "MEET_EMAIL_VERIFY_TIME": 1705392000
   }
   ```

### Q5: 云函数上传失败
**解决方案：**
1. 检查网络连接
2. 确认云开发环境已开通
3. 检查云函数配额是否充足
4. 尝试删除 node_modules 后重新上传

## 四、配置自定义邮箱

如果要使用其他邮箱，修改 `cloudfunctions/mcloud/framework/lib/mail_lib.js`：

```javascript
// 修改默认值
const host = process.env.SMTP_HOST || 'smtp.你的邮箱.com';
const port = Number(process.env.SMTP_PORT || 465);
const user = process.env.SMTP_USER || '你的邮箱@example.com';
const pass = process.env.SMTP_PASS || '你的授权码';
```

或者在云函数环境变量中配置（推荐）：
1. 进入云开发控制台
2. 选择"云函数"
3. 点击 mcloud 函数
4. 进入"配置"标签
5. 添加环境变量

## 五、回滚方案

如果部署后出现问题，可以快速回滚：

### 方案1：使用版本管理
1. 进入云开发控制台
2. 选择"云函数" -> mcloud
3. 点击"版本管理"
4. 选择之前的版本，点击"切换"

### 方案2：恢复备份
1. 从备份中恢复云函数代码
2. 重新上传部署

## 六、监控和维护

### 查看日志
在云开发控制台：
1. 进入"云函数"
2. 选择 mcloud 函数
3. 点击"日志"标签
4. 搜索关键词：
   - `[join]` - 查看预约相关日志
   - `邮件` - 查看邮件发送日志
   - `验证码` - 查看验证码相关日志

### 监控指标
定期检查：
- 邮件发送成功率
- 验证码使用情况
- 云函数调用次数和耗时
- 错误日志

## 七、技术支持

如遇到问题：
1. 查看 `邮件改造说明.md` 了解详细技术实现
2. 检查云函数日志
3. 查看本文档的常见问题部分
4. 联系技术支持

## 八、安全建议

1. **不要将授权码提交到代码仓库**
   - 使用环境变量存储敏感信息
   - 将 `.env` 文件添加到 `.gitignore`

2. **定期更换授权码**
   - 建议每3-6个月更换一次
   - 更换后及时更新配置

3. **监控异常登录**
   - 定期检查邮箱登录记录
   - 发现异常及时处理

4. **限制发送频率**
   - 已实现60秒限流
   - 可根据需要调整限流策略

## 九、性能优化建议

1. **邮件发送异步化**
   - 当前已实现异步发送
   - 不阻塞预约流程

2. **添加邮件队列**
   - 对于高并发场景
   - 可考虑使用消息队列

3. **缓存优化**
   - 验证码记录定期清理
   - 避免数据库膨胀

4. **监控告警**
   - 设置邮件发送失败告警
   - 及时发现和处理问题
