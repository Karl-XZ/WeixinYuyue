# 500错误排查指南

## 错误信息
```
{
  code: 500, 
  msg: "服务器繁忙，请稍后再试", 
  data: {}
}
```

## 排查步骤

### 步骤1：查看云函数日志（最重要！）

1. 打开微信开发者工具
2. 点击顶部"云开发"按钮
3. 进入"云函数"页面
4. 找到 `mcloud` 函数，点击进入
5. 点击"日志"标签
6. 查看最新的错误日志（红色的错误信息）

**请截图或复制完整的错误日志，这是定位问题的关键！**

---

### 步骤2：确认云函数是否已上传

#### 检查方法：
1. 在微信开发者工具中
2. 右键点击 `cloudfunctions/mcloud` 文件夹
3. 选择"上传并部署：云端安装依赖"
4. 等待上传完成（约3-5分钟）

#### 上传时注意：
- 确保选择"云端安装依赖"（不是"不上传node_modules"）
- 等待进度条完成
- 查看控制台输出，确认没有错误

---

### 步骤3：检查路由配置

确认路由是否正确配置：

**文件：** `cloudfunctions/mcloud/project/workfit/public/route.js`

应该包含：
```javascript
'work/email_send_code': 'work/work_home_controller@emailSendCode',
```

---

### 步骤4：检查数据库集合

可能的原因是数据库集合不存在。

#### 解决方案：
1. 进入云开发控制台
2. 点击"数据库"
3. 检查是否存在以下集合：
   - `bx_meet` （教师表）
   - `bx_mail_verify` （验证码表，可能还未创建）

如果 `bx_mail_verify` 不存在，不用担心，第一次使用时会自动创建。

---

### 步骤5：测试邮件发送功能

在本地测试邮件发送是否正常：

```bash
cd cloudfunctions/mcloud
node test_mail.js
```

如果本地测试失败，可能是：
- 授权码错误
- 网络问题
- 163邮箱SMTP服务未开启

---

## 常见错误及解决方案

### 错误1：找不到模块 'nodemailer'

**原因：** 云函数上传时未安装依赖

**解决方案：**
1. 删除云函数
2. 重新上传，选择"云端安装依赖"

---

### 错误2：找不到模块 './mail_verify_service'

**原因：** 新文件未上传

**解决方案：**
1. 确认本地存在以下文件：
   - `cloudfunctions/mcloud/project/workfit/model/mail_verify_model.js`
   - `cloudfunctions/mcloud/project/workfit/service/mail_verify_service.js`
   - `cloudfunctions/mcloud/framework/lib/mail_lib.js`
2. 重新上传云函数

---

### 错误3：SMTP连接失败

**原因：** 邮箱配置错误或网络问题

**解决方案：**
1. 检查 `mail_lib.js` 中的配置
2. 确认授权码正确：`your_smtp_auth_code_here`
3. 确认163邮箱已开启SMTP服务

---

### 错误4：数据库权限问题

**原因：** 云函数没有数据库操作权限

**解决方案：**
1. 进入云开发控制台
2. 点击"数据库"
3. 检查数据库权限设置
4. 确保云函数有读写权限

---

## 快速修复方案

### 方案1：重新上传云函数（推荐）

```bash
# 1. 确保依赖已安装
cd cloudfunctions/mcloud
npm install

# 2. 在微信开发者工具中
# 右键 cloudfunctions/mcloud
# 选择"上传并部署：云端安装依赖"
```

### 方案2：检查文件完整性

确认以下文件存在且内容正确：

- [ ] `cloudfunctions/mcloud/framework/lib/mail_lib.js`
- [ ] `cloudfunctions/mcloud/project/workfit/model/mail_verify_model.js`
- [ ] `cloudfunctions/mcloud/project/workfit/service/mail_verify_service.js`
- [ ] `cloudfunctions/mcloud/project/workfit/controller/work/work_home_controller.js`
- [ ] `cloudfunctions/mcloud/project/workfit/service/work/work_home_service.js`
- [ ] `cloudfunctions/mcloud/project/workfit/public/route.js`

### 方案3：查看详细错误

在云函数中添加更详细的日志：

**临时调试代码：**

在 `work_home_controller.js` 的 `emailSendCode` 方法中添加：

```javascript
async emailSendCode() {
	try {
		console.log('[emailSendCode] 开始执行');
		let rules = {
			email: 'string|must|min:5|max:80|name=邮箱'
		};
		let input = this.validateData(rules);
		console.log('[emailSendCode] 验证通过，邮箱:', input.email);

		let svc = new MailVerifyService();
		console.log('[emailSendCode] 创建服务成功');
		
		let result = await svc.sendRegisterCode(input.email);
		console.log('[emailSendCode] 发送成功:', result);
		
		return result;
	} catch (e) {
		console.error('[emailSendCode] 错误:', e);
		throw e;
	}
}
```

---

## 调试技巧

### 1. 使用云函数测试功能

在云开发控制台：
1. 进入"云函数"
2. 点击 mcloud 函数
3. 点击"测试"标签
4. 输入测试参数：
```json
{
  "$url": "work/email_send_code",
  "email": "test@example.com"
}
```
5. 点击"测试"按钮
6. 查看返回结果和日志

### 2. 本地调试

如果支持本地调试：
1. 在微信开发者工具中
2. 右键云函数
3. 选择"开启云函数本地调试"
4. 在本地运行时可以看到详细日志

---

## 紧急回滚方案

如果问题无法解决，可以临时回滚：

1. 进入云开发控制台
2. 选择"云函数" -> mcloud
3. 点击"版本管理"
4. 选择之前的版本
5. 点击"切换"

---

## 需要提供的信息

为了帮你更快解决问题，请提供：

1. **云函数日志**（最重要！）
   - 完整的错误堆栈信息
   - 错误发生的时间

2. **操作步骤**
   - 你在哪个页面操作的？
   - 点击了什么按钮？
   - 填写了什么内容？

3. **环境信息**
   - 微信开发者工具版本
   - 基础库版本
   - 云开发环境ID

4. **上传状态**
   - 云函数是否上传成功？
   - 上传时有没有错误提示？

---

## 联系支持

如果以上方法都无法解决，请：

1. 截图云函数日志
2. 截图错误提示
3. 说明详细的操作步骤
4. 提供给技术支持

---

**最重要的是查看云函数日志！日志会告诉我们具体是什么错误。**
