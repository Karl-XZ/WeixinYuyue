# 微信预约系统邮件改造说明

## 改造完成内容

### 1. 后端改造（cloudfunctions/mcloud）

#### 1.1 添加邮件发送功能
- ✅ 在 `package.json` 中添加了 `nodemailer` 依赖
- ✅ 创建了 `framework/lib/mail_lib.js` 邮件发送库
- ✅ 邮箱配置（已内置默认值，也可通过环境变量覆盖）：
  - SMTP服务器：smtp.163.com
  - 端口：465
  - 发件邮箱：your_email@163.com
  - 授权码：your_smtp_auth_code_here

#### 1.2 邮箱验证码功能
- ✅ 创建了 `model/mail_verify_model.js` 邮箱验证码数据模型
- ✅ 创建了 `service/mail_verify_service.js` 邮箱验证码服务
  - 发送验证码（60秒限流，10分钟有效期）
  - 验证验证码

#### 1.3 教师模型改造
- ✅ 在 `model/meet_model.js` 中添加了邮箱相关字段：
  - MEET_EMAIL：教师邮箱
  - MEET_EMAIL_VERIFIED：邮箱是否已验证
  - MEET_EMAIL_VERIFY_TIME：邮箱验证时间
- ✅ 移除了 MEET_SUBSCRIBE 订阅消息字段

#### 1.4 教师注册流程改造
- ✅ 在 `route.js` 中添加了 `work/email_send_code` 路由
- ✅ 在 `controller/work/work_home_controller.js` 中添加了 `emailSendCode` 方法
- ✅ 修改了 `workRegister` 方法，增加邮箱和验证码参数
- ✅ 在 `service/work/work_home_service.js` 中：
  - 注册前先验证邮箱验证码
  - 注册时保存邮箱并标记为已验证
  - 移除了订阅消息相关逻辑

#### 1.5 学生预约提醒改造
- ✅ 在 `service/meet_service.js` 中：
  - 移除了订阅消息相关代码
  - 改为邮件提醒：学生预约成功后立即发送邮件给教师
  - 邮件内容包含：预约项目、预约时间、预约人信息

#### 1.6 清理订阅消息
- ✅ 从 `route.js` 中删除了 `subscribe/report` 路由
- ✅ 从相关服务中移除了订阅消息引用

### 2. 前端改造（miniprogram）

#### 2.1 教师注册页面
- ✅ 在 `work_register.js` 中：
  - 添加了邮箱、验证码输入字段
  - 添加了发送验证码功能（带60秒倒计时）
  - 修改了注册提交逻辑
- ✅ 在 `work_register.wxml` 中：
  - 添加了邮箱输入框
  - 添加了验证码输入框和发送按钮
- ✅ 在 `work_register.wxss` 中：
  - 添加了验证码按钮样式

#### 2.2 业务逻辑层
- ✅ 在 `biz/work_biz.js` 中：
  - 移除了 SubscribeBiz 引用
  - 修改了 workRegister 方法，增加邮箱和验证码参数
  - 移除了登录/注册后的订阅消息提示

## 安装步骤

### 1. 安装依赖
在云函数目录执行：
```bash
cd cloudfunctions/mcloud
npm install
```

### 2. 上传云函数
在微信开发者工具中：
1. 右键点击 `cloudfunctions/mcloud` 文件夹
2. 选择"上传并部署：云端安装依赖"

### 3. 配置环境变量（可选）
如果需要使用不同的邮箱配置，可以在云函数环境变量中设置：
- SMTP_HOST：SMTP服务器地址
- SMTP_PORT：SMTP端口
- SMTP_SECURE：是否使用SSL（true/false）
- SMTP_USER：发件邮箱
- SMTP_PASS：邮箱授权码
- MAIL_FROM：发件人显示名称

### 4. 数据库迁移
由于修改了 meet 表结构，已有教师数据的邮箱字段会是空值。建议：
- 新教师注册时会自动填写邮箱
- 老教师可以通过后台管理或提供一个"绑定邮箱"功能来补充

## 功能说明

### 教师注册流程
1. 教师填写姓名、手机、密码、教师密码
2. 填写邮箱地址
3. 点击"发送验证码"按钮，系统发送6位数字验证码到邮箱
4. 填写收到的验证码
5. 提交注册，系统验证验证码后完成注册

### 学生预约提醒
1. 学生成功预约课程后
2. 系统自动读取该课程教师的邮箱
3. 如果教师邮箱已验证，立即发送邮件提醒
4. 邮件内容包含：预约项目、预约时间、预约人信息

## 注意事项

1. **邮箱配置**：当前使用的是163邮箱，授权码已内置。如需更换邮箱，请修改环境变量或 `mail_lib.js` 中的默认值。

2. **验证码有效期**：邮箱验证码10分钟内有效，60秒内不能重复发送。

3. **邮件发送失败**：如果邮件发送失败，不会影响预约流程，只会在日志中记录警告。

4. **老数据兼容**：已有的教师数据邮箱字段为空，不会收到邮件提醒。需要补充邮箱信息。

5. **订阅消息清理**：已移除所有订阅消息相关代码，包括：
   - subscribe_controller.js 和 subscribe_service.js 的引用
   - msg_service.js 中的订阅消息发送逻辑
   - 前端的 SubscribeBiz 引用

## 测试建议

1. 测试教师注册流程：
   - 验证邮箱格式检查
   - 验证码发送和验证
   - 注册成功后登录

2. 测试学生预约流程：
   - 学生预约成功
   - 教师邮箱收到提醒邮件
   - 邮件内容正确

3. 测试异常情况：
   - 邮箱格式错误
   - 验证码错误
   - 验证码过期
   - 邮件发送失败（不影响预约）

## 后续优化建议

1. 为老教师提供"绑定邮箱"功能
2. 添加邮件模板，支持HTML格式的美化邮件
3. 添加邮件发送记录，便于追踪
4. 支持多种邮件服务商（QQ邮箱、Gmail等）
5. 添加邮件发送队列，提高可靠性
