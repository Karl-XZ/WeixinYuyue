# 环境检查结果

## ✅ 云环境ID检查

### 后端配置（云函数）
**文件：** `cloudfunctions/mcloud/config/config.js`
```javascript
CLOUD_ID: 'your_cloud_environment_id'
```

### 前端配置（小程序）
**文件：** `miniprogram/setting/setting.js`
```javascript
CLOUD_ID: 'your_cloud_environment_id'
```

### 结论
✅ **云环境ID一致，配置正确！**

---

## 🔍 500错误可能的原因

既然环境ID正确，那么500错误可能是：

### 1. 云函数未上传到正确的环境

**检查方法：**
1. 打开微信开发者工具
2. 点击顶部"云开发"按钮
3. 确认当前环境是：`your_cloud_environment_id`
4. 查看"云函数"列表，确认 `mcloud` 函数存在

**解决方案：**
- 确保在正确的环境中上传云函数
- 右键 `cloudfunctions/mcloud`
- 选择"上传并部署：云端安装依赖"

---

### 2. 云函数版本未更新

**检查方法：**
1. 进入云开发控制台
2. 点击"云函数" -> mcloud
3. 查看"版本管理"
4. 确认最新版本的更新时间

**解决方案：**
- 如果更新时间是今天之前的，说明未更新
- 重新上传云函数

---

### 3. 云端依赖未安装

**检查方法：**
查看云函数日志，如果看到：
```
Error: Cannot find module 'nodemailer'
```

**解决方案：**
1. 删除云函数
2. 重新上传，**必须选择"云端安装依赖"**

---

### 4. 新文件未上传

**检查方法：**
查看云函数日志，如果看到：
```
Error: Cannot find module './mail_verify_service'
```

**解决方案：**
- 确认本地文件存在
- 重新上传云函数

---

## 📋 详细排查步骤

### 步骤1：确认云函数已上传

在微信开发者工具中：

1. 点击"云开发"
2. 选择环境：`your_cloud_environment_id`
3. 进入"云函数"页面
4. 查看 `mcloud` 函数是否存在
5. 查看最后更新时间

### 步骤2：查看云函数日志

1. 点击 `mcloud` 函数
2. 进入"日志"标签
3. 查看最新的错误日志
4. **重点关注红色的错误信息**

### 步骤3：测试云函数

在云开发控制台：

1. 进入"云函数" -> mcloud
2. 点击"测试"标签
3. 输入测试参数：
```json
{
  "$url": "work/email_send_code",
  "email": "test@example.com"
}
```
4. 点击"测试"按钮
5. 查看返回结果

**预期结果：**
```json
{
  "ok": 1,
  "expireSeconds": 600
}
```

**如果失败：**
- 查看错误信息
- 查看日志输出

---

## 🎯 快速解决方案

### 方案1：完全重新部署（推荐）

```bash
# 1. 本地确认文件完整
cd cloudfunctions/mcloud
node check_files.js

# 2. 确认依赖已安装
npm install

# 3. 在微信开发者工具中
# - 右键 cloudfunctions/mcloud
# - 选择"删除云端文件和函数"（如果有）
# - 再次右键 cloudfunctions/mcloud
# - 选择"上传并部署：云端安装依赖"
# - 等待上传完成（3-5分钟）
```

### 方案2：检查云函数配置

在云开发控制台：

1. 进入"云函数" -> mcloud
2. 点击"配置"标签
3. 检查：
   - 运行环境：Node.js 12 或以上
   - 内存：256MB 或以上
   - 超时时间：20秒 或以上

---

## 📞 需要提供的信息

为了准确定位问题，请提供：

### 1. 云函数日志（最重要！）

在云开发控制台查看，包括：
- 完整的错误堆栈
- 错误发生时间
- 请求参数

### 2. 云函数状态

- 最后更新时间
- 当前版本号
- 运行环境

### 3. 操作步骤

- 在哪个页面操作？
- 点击了什么按钮？
- 填写了什么内容？

---

## 🔧 调试命令

### 本地检查
```bash
# 检查文件完整性
cd cloudfunctions/mcloud
node check_files.js

# 测试邮件发送
node test_mail.js
```

### 云端测试
在云开发控制台的"云函数测试"中：

**测试发送验证码：**
```json
{
  "$url": "work/email_send_code",
  "email": "your-email@example.com"
}
```

**测试教师注册：**
```json
{
  "$url": "work/register",
  "name": "测试教师",
  "phone": "13800138000",
  "pwd": "123456",
  "teacherPwd": "123456",
  "email": "test@example.com",
  "emailCode": "123456"
}
```

---

## ✅ 检查清单

部署前请确认：

- [ ] 本地文件完整（运行 `node check_files.js`）
- [ ] nodemailer 已安装（运行 `npm install`）
- [ ] 云环境ID正确（`your_cloud_environment_id`）
- [ ] 云函数已上传到正确环境
- [ ] 选择了"云端安装依赖"
- [ ] 上传完成无错误
- [ ] 云函数日志无错误

---

**下一步：请查看云函数日志，把错误信息发给我！**
